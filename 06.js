// @flo

/*
  - Optional properties
  - maybe types
  - mixed types
  - enumerated types
*/

type myObject = {
  myNumber: number,
  myString: string,
  myBool?: boolean,
}

var aString = 'string'

var obj: myObject = {
}
