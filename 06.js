// @flow

type myObject = {
  myNumber: number,
  myString: string,
  myBool: boolean,
  myFunc: Function,
  // myComplexFunc:
  // genericFunc:
}

var obj: myObject = {}

// function f<A,B>(a: A, b: B): string {
// }
